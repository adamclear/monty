#include "monty.h"

/**
 * nop - no operation, does nothing. NOTHING AT ALL
 * @stack: the stack
 * @line_number: line number
 * Return: void
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
}
